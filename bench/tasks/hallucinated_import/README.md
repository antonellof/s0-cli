# hallucinated_import

Tests the agentic harness's ability to detect that an `import` references a package that's not in the lockfile and not stdlib. Pure LLM signal — no classic scanner reliably catches this.
