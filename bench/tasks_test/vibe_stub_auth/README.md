# vibe_stub_auth

Tests vibe-code detection. semgrep is unlikely to flag string-equality auth checks reliably; the agentic harness should catch them via the prompt's vibe-code rubric. Single-shot may miss it — that's OK and reflects the cost/accuracy trade-off.
